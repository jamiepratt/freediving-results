# 2025 CMAS indoor JSON result ingestion

The 2025 Athens [CMAS event page](https://www.cmas.org/freediving-events/2025-cmas-world-championship-freediving-indoor.html) links the Microplus timing application. The source archive retains exact `CGR1` response bytes and cites the generated HTML result pages for Static Apnea (STA), Speed Apnea 2x50, 4x50 and 8x50, DNF, Dynamic Apnea Bi Fins (DYNBF), and Dynamic Apnea (DYN), each across six junior, senior and master women and men categories. The [STA Masters Men page](https://results.microplustimingservices.com/CMAS/Results/#/1/static-result-json/MAM/001/007/001), [Speed Apnea Masters Men page](https://results.microplustimingservices.com/CMAS/Results/#/1/speed-result-json/MAM/004/007/001), [DNF Masters Men page](https://results.microplustimingservices.com/CMAS/Results/#/2/dynamic-result-json/MAM/011/007/001), [DYNBF Masters Men page](https://results.microplustimingservices.com/CMAS/Results/#/2/dynamic-result-json/MAM/016/007/001), and [DYN Masters Men page](https://results.microplustimingservices.com/CMAS/Results/#/2/dynamic-result-json/MAM/026/007/001) are examples. `freediving.cmas-2025-indoor-json` parses valid UTF-8 responses into source headers and rows. The JSON endpoint remains a distinct acquisition URL. The parser binds the page route to the JSON category, competition, round, heat, sport and filename. Version `/9` checks Speed Apnea 2x50 and 8x50 against May 22, 4x50 and STA against May 23, DNF against May 20, DYNBF against May 21, and DYN against May 24, 2025. Speed, STA, DYNBF and DYN require round `007` and heat `001`. Earlier parser versions remain replayable. Invalid UTF-8, changed result structure and mismatched provenance fail closed.

The DNF Masters Men example response has 30 rows. Each preserves its source `PlaCod`, heat, lane, name, representation, category, `MemPrest` and `MemPoint` tokens. The raw row remains intact. This parser does not assign an athlete identity, interpret `PlaCod` as a globally unique person key, combine Results and Summary views, or approve a sporting attempt. Every candidate is unreviewed and blocked from selection. A known `PlaCod` collision in another retained 2025 view prevents global uniqueness assumptions. The B24 Seniors Women DNF response contains an invalid UTF-8 byte; the exact archived source and byte span permit only its one malformed row to remain explicitly unparsed. Other invalid bytes fail strict decoding. Other 2025 JSON views and the unavailable configured PDFs remain separate source gaps in [issue #8](https://github.com/jamiepratt/freediving-results/issues/8).

Private candidate packets label schema-5 observations as JSON and anchor each to its zero-based source row and the HTML result page URL.

On 2026-09-25, six DYNBF `016` CGR1 response hashes matched an earlier independent JSON audit. Their source rows total 177: JUF 4, JUM 10, MAF 15, MAM 32, SEF 54 and SEM 62. Version `/4` imported each source position once into a separate private B25 database restored from the B24 backup. The resulting database has 13 extraction versions, 12 source hashes, 368 versioned observations and 338 source-position candidate IDs. All 177 new candidates remain unreviewed and selection-blocked. The prior B24 SEF invalid-byte exception remains preserved in its original extraction, without a guessed surname. The ignored private B25 packet retains exact response bytes, headers, acquisition receipts, derivations and a database backup. These counts describe source rows and extraction versions, not distinct sporting attempts or owner-approved results.

On 2026-09-25, six DYN `026` CGR1 views supplied 169 desktop source rows: JUF 4, JUM 8, MAF 12, MAM 33, SEF 55 and SEM 57. Each responsive mobile table duplicated its desktop rows. Every desktop row matched the independently acquired JSON at its original position, including heat, rank or status, nation, name and distance token. Version `/5` imported all 169 positions with no exceptions into a private B26 database restored from B25. The database has 19 extraction versions, 18 source hashes, 537 versioned observations and 507 source-position candidate IDs. All 169 new candidates remain unreviewed and selection-blocked. All 19 versions replay and skip on exact retry. The B24 malformed DNF SEF row remains quarantined in its original extraction. The ignored `data/b26-2025-dyn/` packet retains source bytes, HTTP headers, provenance, derivations, the B25 backup and the B26 backup. These counts do not establish distinct attempts, reviewed results or complete championship coverage.

On 2026-09-25, six Static Apnea `001` CGR1 views used the separate `CMAS_1` swimming route, `#/1/static-result-json/<CAT>/001/007/001`, and `NU<CAT>001CLAS07 001.JSON` responses. Chrome reconciled 129 desktop rows against the original JSON positions: JUF 1, JUM 7, MAF 10, MAM 26, SEF 42 and SEM 43. Each responsive mobile table repeated the desktop rows. The result pages displayed `Finished` on May 23, 2025. `MemPrest` contains the displayed Time token or literal `DSQ`; 12 rows are `DSQ`. `MemPoint` and `MemQual` are empty in every row. Parser `/6` preserves those tokens and marks unit meanings unknown, without converting time text into metres or seconds. All 129 rows imported into a separate private B27 database restored from B26; the six new versions have no unparsed rows and remain unreviewed and selection-blocked. The B27 database has 25 extraction versions, 24 source hashes, 666 versioned observations and 636 source-position candidate IDs. Earlier versions, including the B24 malformed DNF SEF row quarantine, remain preserved and replayable. The ignored `data/b27-2025-sta/` packet retains exact bytes, HTTP headers, provenance, source-position ledger, archive, verification scripts and the isolated database backup. These are versioned source rows, not distinct approved sporting attempts or complete event coverage.

Six Speed Apnea 8x50 `004` CGR1 views use `#/1/speed-result-json/<CAT>/004/007/001` and `NU<CAT>004CLAS07 001.JSON`. Their source rows total 41: JUF 3, JUM 6, MAF 3, MAM 7, SEF 10 and SEM 12. The page's Time column corresponds to exact `MemPrest` clock text in 37 rows; `MemFields[8].V` repeats the finish clock. Four rows have status tokens, three `DSQ` and one `DNS`, with blank nested finish text. `MemPoint` and `MemQual` are blank. Intermediate `MemFields` entries contain split text, sometimes blank or irregular, and remain raw source data rather than derived results. Parser `/7` accepts exact route and header binding, preserves clock and status tokens without converting them to metres or a numeric duration, and quarantines any row whose finish contract is ambiguous at its original JSON position. All candidates remain unreviewed and selection-blocked. This paragraph describes source semantics, not owner-approved results.

On 2026-09-25, the exact JSON response (19,407 bytes, SHA-256 `3e34c617c4975368bc9ddde0e0c9cef7dd8e9d05a87c5f0f86a22c04572b87d8`) matched an earlier retained acquisition. Two private extraction versions preserve a pre-publication source-name correction: `/1` retained given-name-first text, while `/2` follows the generated HTML's surname-first display. Both versions replay and exact retries skip. The current `/2` artifact SHA-256 is `cc187f24c0eb156e4a05174acf915cb40bfcf43682a0dfd1f459419351557e06`. The isolated `championship_b22_json` database has two versions of the same 30 source rows: 60 unreviewed observation versions, 30 source-position candidates, one source hash. Every `/2` raw row and row coordinate reconciled to the source; replay passed for both versions. The database backup SHA-256 is `a4050c828add8949221e7963775149a20675660fbf04b8bba663a796fd1d2c0c`. Private bytes, manifest, archive and backup are under `data/b22-2025-json-evidence/` in the B22 checkout. No production data, owner review, policy, selection or deployment changed.

Six Speed Apnea 2x50 `002` CGR1 Results views use `#/1/speed-result-json/<CAT>/002/007/001` and `NU<CAT>002CLAS07 001.JSON`. The [Masters Men result page](https://results.microplustimingservices.com/CMAS/Results/#/1/speed-result-json/MAM/002/007/001) illustrates the source citation. All six views show `Finished` on May 22, 2025. Chrome reconciled 60 logical desktop rows to original zero-based JSON positions; each responsive mobile table duplicated its desktop rows. Category counts are JUF 5, JUM 6, MAF 6, MAM 14, SEF 15 and SEM 14. Every displayed Time equals `MemPrest`: 53 clock tokens, two `DSQ`, five `DNS`. For clock rows, `MemFields[2].V` repeats the displayed Time. The two Masters Men `DSQ` rows at positions 3 and 10 contain nested clocks `57.77` and `47.93`; the displayed status remains `DSQ`. Four clock rows have a blank first split. All nine `MemFields` entries remain raw evidence; no split, finish, unit or attempt status is inferred from blank or conflicting nested values. `MemPoint` and `MemQual` are blank throughout. Parser `/8` binds the exact route and header and quarantines structurally ambiguous rows at their original positions.

The ignored private B29 packet `data/b29-2025-2x50/` retains exact fresh HTTP response bytes, headers, retrieval instants, source-position ledger and the B28 archive and backup. Its six new sources supplied 60 unreviewed, selection-blocked candidates with zero new exceptions. The isolated B29 database restored from B28 has 37 extraction versions, 36 source hashes, 767 versioned observations and 737 source-position candidate IDs. All 37 versions replay and skip on exact retry. B24's malformed DNF SEF row remains quarantined in its original extraction at source position 19. An independent B29 backup restore reproduced all 37 extractions and 767 observations with matching ordered payload digests. These are source rows and extraction versions, not distinct approved sporting attempts or complete championship coverage.

Six Speed Apnea 4x50 `003` CGR1 Results views use `#/1/speed-result-json/<CAT>/003/007/001` and `NU<CAT>003CLAS07 001.JSON`. The [Masters Men result page](https://results.microplustimingservices.com/CMAS/Results/#/1/speed-result-json/MAM/003/007/001) illustrates the citation. All six show `Finished` on May 23, 2025. A browser audit matched 54 logical desktop rows to the original JSON positions on displayed rank or status, lane, nation, name and Time; the mobile table on each page repeats those rows. Category counts are JUF 5, JUM 6, MAF 4, MAM 9, SEF 17 and SEM 13. The Time column matches `MemPrest`: 50 clock tokens, two `DSQ` and two `DNS`. `MemFields[4].V` matches all 50 displayed clocks. Masters Men `DSQ` at position 8 carries nested `2:27.40`, which remains raw evidence rather than a finish. Missing and unusual intermediate splits remain raw; no time-to-metres or numeric-duration conversion is made. Parser `/9` binds the exact route, header, date and filename and quarantines structurally ambiguous rows at their original positions. Direct visits to the CMAS event page returned HTTP 403 during this acquisition; the previously retained federation link and the indexed official event page establish the discovery context, without a claim of fresh direct federation link inspection.

The ignored private B30 packet `data/b30-2025-4x50/` retains the exact six HTTP responses, headers, UTC retrieval instants, row ledger, copied B29 archive and backup, and a new isolated database backup. All 54 new positions imported with zero exceptions and remain unreviewed and selection-blocked. The database restored from B29 has 43 extraction versions, 42 source hashes, 821 versioned observations and 791 source-position candidate IDs. All 43 versions replay and skip on exact retry; the B24 malformed DNF SEF row remains quarantined. An independent B30 backup restore reproduced all 43 extractions and 821 observations with matching ordered payload digests. The B30 backup SHA-256 is `38c96a9262ea7f369c390f8e74c2f7b7b604b4645e9d84f02904f8bec85148e9`. These counts describe source rows and extraction versions, not distinct approved sporting attempts or complete championship coverage.

The B30 archive's 42 distinct, hash-verified CGR1 objects contain 792 source positions across the seven disciplines and six category codes. The [dated inventory](championship-inventory-20260925.md#athens-2025-indoor-acquired-view-reconciliation) records each category count and source session date. All positions in **these 42 discovered views** are imported or explicitly quarantined; event-wide category/session completeness is still unresolved. The 2025 configured indoor PDFs returned 404. Direct CMAS federation-page visits returned 403 during B30, so the earlier retained federation link supplies discovery provenance rather than fresh link verification. The [historical mirror audit](athens-mirror-audit-20260925.md) retains its material disagreements and unresolved diagnostic rows; its PDF is separate supporting evidence, with no approved row correspondence or supersession.

A separate ignored private owner packet at `data/b31-2025-dnf-mam-review/` prepares all 30 zero-based positions of the B22 DNF Masters Men `TFMAM011` Results view for exact-source extraction review. `packet.md` (SHA-256 `8000769d9c70f50e194c65cee0d235a529fda8f0c16b7e334460c1336daba0f6`) gives each row's human-facing HTML result citation and displayed comparison. `review-rows.jsonl` (SHA-256 `341a00b6c33e119cee5ab0babc85b2d8f5d4bbaec615a87371ff3a6e8bcdf4e4`) retains full raw JSON row fields and blank owner decision fields. The copied response remains 19,407 bytes with the B22 SHA-256 above. A live Chrome check found 30 desktop rows plus a duplicate mobile table; all 30 desktop rows matched the JSON display tokens at their original positions, with five literal `DSQ` statuses and 25 displayed distance tokens, including two literal `0.00` values. A row-level historical-mirror pairing was unavailable, so the packet flags that comparison as unestablished per row. No owner attestation, identity decision, attempt merge, source-completeness approval or publication approval is recorded. Further review and coverage decisions remain in [issue #8](https://github.com/jamiepratt/freediving-results/issues/8).
