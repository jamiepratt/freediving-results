# Source acquisition and row lineage

Route baseline observed through 25 September 2026, with remote batch evidence through 26 September. This is a route and evidence guide, not a live availability check or a complete championship inventory. Start at the [CMAS results archive](https://www.cmas.org/freediving/results.html) or the AIDA event page, then retain what the route actually returns. Current source coverage and unresolved work live in [issue #8](https://github.com/jamiepratt/freediving-results/issues/8).

## Remote 2025-onward discovery batch

[Issue #16](https://github.com/jamiepratt/freediving-results/issues/16) owns the wider 2025-onward sweep. Its dated inventory must state the cutoff and each checked index, event, session, discipline, category and candidate result link. Each candidate needs one of `imported`, `already present/duplicate`, `unsupported`, `inaccessible`, `not final` or `unresolved`, with a reason and retrieval evidence. An index scan does not establish a global census. Check the existing [championship inventory](championship-inventory-20260925.md) and private source hashes before fetching to avoid duplicate #8 acquisitions.

Run live discovery, browser capture and import only as a dedicated ingestion OS account, with a private archive and a separate restricted PostgreSQL corpus. For the OVH batch chosen by the owner, `scripts/setup-ingestion-worker.sh --apply` creates `freediving-ingest`, private `/var/lib/freediving-ingest` and `/srv/freediving-ingest` paths, and a separate `freediving_ingest` database with `freediving_ingest_owner` and `freediving_ingest_app` roles on the existing PostgreSQL server. It does not change the public application database or service. Run `--describe` first to inspect its fixed names. `--apply` needs root, unattended sudo access to local `postgres`, and working PostgreSQL loopback password authentication. It is idempotent: an existing account, path, role or database with unexpected identity or ownership makes it stop. Both roles lack cluster administration privileges. The app role has no memberships or database ownership, as required by `freediving.observations/migrate!`. Passwords and JDBC environment files remain mode 0600 under the worker state directory. Source `/var/lib/freediving-ingest/database-owner.env` only for migration and `/var/lib/freediving-ingest/database.env` for imports. Never print either file or put it in a command argument, Git file or portable bundle. The worker owns its own archive, runs, bundle directory and shared SQLite lease at `/var/lib/freediving-ingest/acquisition.sqlite3`. Run bounded acquisition under that account; do not read or change `/opt/freediving/current`, `/etc/freediving`, the public database, service, tunnel, release symlinks or publication policy.

Before the first publisher request, retain a private preflight record with the actual host/environment, Git commit, UTC cutoff, Python, pinned package, Playwright Chromium, Java, Poppler and PostgreSQL versions, isolated database name, lease path, archive path and bundle destination. Install Python 3.12 with the pinned requirements and Chromium, plus `poppler-utils` (`pdftotext`, `pdfinfo`) and the portable Clojure CLI and Java 17+ required by archive, extraction and observations commands. Record versions after install. Stop if the account, database login, browser binary or private output paths are missing. Bound process concurrency and memory on the shared OVH host to protect the public service. Keep backups and bundles outside the checkout. Export only the archive, ledgers, preflight and custom database backup; exclude `/var/lib/freediving-ingest/db-*-password`, `database*.env`, browser storage state and all other credential files. Verify bundle paths and contents contain no credentials or session state.

Use Python 3.12 and `requirements-acquisition.txt` in a worker virtual environment, then run `python -m playwright install chromium` and record `python -m playwright --version` and the installed Chromium revision. Browser system packages remain worker provisioning, outside Git. Start with unauthenticated access from that worker. If a publisher requires existing browser state, confirm the exact origin and expiry, then place only that origin's needed cookies and storage in an owner-readable Playwright storage-state file outside Git. Recheck the selected result view. Never export an entire Chrome profile. Do not include the storage-state file, database credentials or API keys in logs, manifests or portable bundles.

Use the shared SQLite lease path for every worker and the existing `acquire_source.py` and `capture_browser.py` routes. Record requested, discovery and final URLs, redirects, selected date/filter, content type, byte count, SHA-256, complete original bytes and rendered DOM hash. A browser capture requires the selected date/filter to match both rendered controls and result-bearing responses before registering it. Failed or partial responses remain explicit gaps. For each complete source, register an archive manifest, extract with the matching PDF/HTML/JSON parser, import into the isolated database, and use the `source-run/v1` `import-once` checkpoint with the immutable extraction job ID. Replay the same inputs and verify no new acquisition or observation versions. Compare original result positions with parsed and unresolved positions; versioned observation totals are not unique sporting attempts. Preserve uncertain mirror/revision relationships.

For a bounded configured index set, fill in `config/source-discovery.example.json` with actual publisher URLs and run `python3 scripts/discover_results.py CONFIG /ABS/PRIVATE/OUTPUT`. The private `inventory.json` and `archive/` contain dated candidate decisions and original receipts. Repeating the command reuses verified bytes; `--retry-gaps` retries prior gaps. The first version recognizes HTML links with `data-date="YYYY-MM-DD"` and JSON `results` entries with date, URL and representation. Other publisher layouts need explicit adapter work and an `unsupported` or `unresolved` inventory entry; this command does not import rows or claim the index set is globally complete.

Export the private archive, row and gap ledgers, preflight record and isolated PostgreSQL custom backup with `portable_corpus.py`; verify the bundle and scan its file names and contents for session values and credentials before handoff. Leave identity review, source validation and publication decisions separate. Record actual batch counts, citation checks and remaining gaps on #16 while keeping it open until the full dated scope passes its acceptance criteria.

### CMAS World Games Series, March 2025 remote batch

On 26 September 2026, isolated OVH run `20260926-b02` checked the official [CMAS results index](https://www.cmas.org/freediving/results.html), the [World Games Series document page](https://www.cmas.org/document/freediving/freediving-results/world-games/world-games-series-freediving.html), and its [PDF](https://www.cmas.org/document/world-games-series-freediving/download.html) through the shared CMAS lease. The index exposed 15 links titled as 2025 results. This batch imported one distinct document; the other 14 remain unresolved in the private inventory, with prior corpora to check before any future fetch. This is a bounded index check, not a global census.

The official 34-page PDF is 408,344 bytes, SHA-256 `d8f13447d1289726ef635deb00da142157533c71870faf4e6cf7032bb37b9f5e`. Parser `cmas-world-games-series-2025-03/1` reads only final heat result tables on PDF pages 11, 12, 18, 19, 25, 26, 32 and 33. It imports 29 cited rows, including five printed DSQs, across men's and women's DNF and DYN on 29-30 March 2025. Entry lists, summary rankings and medallist pages remain supporting views and do not create more attempts. Each of the 29 source positions matched the isolated database row's page, line, exact text and status. Repeated extraction and `source-run/v1` import skipped; the isolated corpus then held two extraction jobs and 67 observation versions including the preceding August World Games Day 1 run. These counts are versioned observations, not a cross-corpus unique-attempt count.

Private originals, row ledger and inventory remain in `/srv/freediving-ingest/runs/20260926-b02`; the shared archive is `/srv/freediving-ingest/archive`. The custom PostgreSQL backup and verified portable bundle are `/srv/freediving-ingest/runs/20260926-b02/database.dump` and `/srv/freediving-ingest/bundles/20260926-b02`. The bundle excludes original HTML index bytes because those public pages embedded ephemeral CSRF fields; the originals remain private on the worker. The bundle passed path, hash and credential scans. The preflight facts were checked before acquisition, but the private preflight file was written after the first three requests, as recorded there. Source revision relationships, identity review and publication remain unresolved under [issue #16](https://github.com/jamiepratt/freediving-results/issues/16).

### CMAS Kaohsiung Indoor World Cup, September 2025 remote batch

On 26 September 2026, isolated OVH run `20260926-b03` reused the verified CMAS results-index receipt from b02, retrieved at 11:45:49 UTC. It did not recheck index freshness. The worker wrote its private preflight at 12:07:20 UTC before its one new publisher request. The [official Kaohsiung resultbook](https://www.cmas.org/document/2025,-cmas-world-cup-freediving-indoor/download.html) returned HTTP 200, `application/pdf`, 138,612 bytes, SHA-256 `1e58fefbe81475c620bf80a8ebec25c697129c389abd31495371500d3459a8e9`, with no redirect or retry. The exact source identity was absent from the b01 and b02 worker acquisitions and the checked #8 inventory. The 15-link reused index inventory now marks the March Series and Kaohsiung imported; 13 other links remain unresolved within this bounded check.

Parser `cmas-kaohsiung-world-cup-2025/1` accounts for 81 final result positions on PDF pages 1-6, with page counts 14/14/12/13/13/15. All 81 parsed and imported with exact page, line, text and printed-status matches against an independent source ledger. The printed statuses include two `DQBO-SURFACE`, two `DQSP` and 15 `DNS` rows. The wrapped Raymond KO penalty note is retained with its row. Pages 7-8 are aggregate rankings, not additional attempts. The isolated corpus now has three extraction jobs and 148 observation versions; unique sporting attempts across corpora are not asserted. An unchanged acquisition reused local bytes, extraction skipped the same artifact, and `source-run/v1` skipped import.

Private evidence and custom PostgreSQL backup are in `/srv/freediving-ingest/runs/20260926-b03`; the verified 26-payload-file portable bundle is `/srv/freediving-ingest/bundles/20260926-b03`, index SHA-256 `d1ae518532d5649a25ed29dadee3b88a6762b30d0f85dd1c86aed17f8a56be79`. The bundle excludes original index HTML with ephemeral CSRF fields and passed path, credential and session scans. Revision relationships, the wider 2025-onward sweep, identity review and publication remain unresolved in [issue #16](https://github.com/jamiepratt/freediving-results/issues/16).

### CMAS Łódź Indoor World Cup, March 2025 remote batch

On 26 September 2026, isolated OVH run `20260926-b04` reused the verified CMAS results index captured in b02; index freshness was not rechecked. The private worker preflight was corrected and recorded at 12:29:10 UTC before the only publisher request. The index's Unicode Łódź URL first produced a local `unsafe_redirect` diagnostic without a network request; percent encoding the same link succeeded through the shared CMAS lease. The [official PDF](https://www.cmas.org/document/2025-cmas-world-cup-freediving-indoor-%C5%82o%CC%81dz%CC%81,-poland/download.html) returned HTTP 200, `application/pdf`, 824,128 bytes, SHA-256 `b1104d5d3e6f1bfee1c627cc2134949a9766fea18273a2c9dbbd96423f6f3a97`, in one attempt with no redirect or retry. Prior b01-b03 worker receipts had no exact link match. The dated #8 inventory was reviewed, but its private archive was unavailable for an exhaustive byte-level comparison.

Parser `cmas-lodz-indoor-world-cup-2025/1` accounts for 107 printed result positions on March 14-16 (31/37/39). All 107 parsed and imported with independent page, line, exact text, printed card and date matches. Ten rows have RED cards and three have printed para categories; raw result, points, remarks and category strings remain intact. Repeated athlete appearances retain distinct source positions without asserting cross-source sporting identity. Unchanged acquisition reused the original bytes, extraction skipped the same job, and `source-run/v1` skipped import. The isolated corpus has four extraction jobs and 255 observation versions, not an established unique-attempt count. The reused 15-link index inventory now marks three links imported and twelve unresolved; this is not a global 2025-onward census.

Private originals, row ledger, inventory and custom PostgreSQL backup are in `/srv/freediving-ingest/runs/20260926-b04`. The verified 34-file portable bundle is `/srv/freediving-ingest/bundles/20260926-b04`, index SHA-256 `4282bb896a2a8a8acc74315b129a8adf5c718ae6757fe67c8ebf1c96363cc6a6`. It excludes original index HTML with ephemeral CSRF fields and passed credential and session scans. The exact version strings missing from the initial preflight were supplemented after the request, with that timing retained in the record. Index freshness, remaining links, source revisions, identity review and publication stay open under [issue #16](https://github.com/jamiepratt/freediving-results/issues/16).

### Four more official CMAS 2025 PDFs, September 2026 remote batch

On 26 September 2026, isolated OVH run `20260926-b07-cmas-index` refreshed the [official CMAS results index](https://www.cmas.org/freediving/results.html) through the shared lease. The fresh response was HTTP 200, `text/html`, 261,617 bytes, SHA-256 `09100973cf195289dbb5ff95237c11c5e4dfc7f7078ec0ffc2576b51f007f1d6`. Its 15 official 2025 download links matched b02 exactly. The run cutoff was 13:43:39 UTC. Preflight was retained before the five publisher requests; the exact Chromium version was recorded in a post-request supplement. Every request succeeded on its first attempt, with no redirect or retry.

| Official PDF | Bytes, SHA-256 | Extraction status |
| --- | --- | --- |
| [Camotes World Cup depth](https://www.cmas.org/document/2025-cmas-world-cup-freediving-depth-camotes,-philippines/download.html) | 656,529; `298e408b4b71afe5e35c01a76784e5d06f8c7e217df171cd5d9e171682492736` | Four scanned result pages, no extractable text. Archived; row count unknown pending OCR and independent reconciliation. |
| [Italian Open Outdoor 2025](https://www.cmas.org/document/2025,-italian-open-outdoor-freediving-championship/download.html) | 13,755,700; `e8ca68825e29d44a1b13127ab2cab55f6db534b15ae26ff005f13a1ca8ec9d44` | 35 pages, of which 33 have no extractable text. The two text pages contain rankings. Archived; source result-row count unknown. This is distinct from the imported 2026 Italian Open source. |
| [UNU Tampa Bay Challenge](https://www.cmas.org/document/2025-unu-tampa-bay-freediving-challenge/download.html) | 272,530; `61831ff0715a2130e2c2d35aa6caac30d92fc9f795a7cee3c290782710949781` | 36/36 printed positions imported across three pages, with exact page, line and text matches. The printed static date `06.12.2025` remains unconverted because its ordering is ambiguous; dynamic pages print no date. |
| [nOxyCup indoor](https://www.cmas.org/document/2025,-cmas-noxy-international-cup/download.html) | 1,913,763; `d544097ae251ec08801200f86aeaa95c0f90fda56c294970f87302664e85f3d2` | 104/104 printed positions imported across five pages, including a one-line DNS continuation. Exact page, line and text matched the independent ledger. |

All four returned HTTP 200 `application/pdf` at their requested URLs. Original bytes, complete acquisition receipts, source versions and unknown revision relationships remain in the private run. The two supported parsers retain original values, notes, statuses, category and unit uncertainty. Rendered page-one checks covered both imported PDFs. Repeated acquisition reused local bytes, extraction skipped the same jobs, the `source-run/v1` checkpoints stayed complete, and direct database imports skipped. The isolated corpus now holds eight jobs and 824 versioned observation rows, up from six and 684. These are not established unique sporting attempts.

The private 15-link ledger marks five imported (three earlier, two here), five already inventoried in #8, two unsupported scans, and three unrequested links: Deep Dominica, Vertical Blue and Camotes Challenge. The five #8 links are known discovery routes, not proven byte duplicates of this worker's archive. #8's private championship bytes were unavailable for exhaustive comparison. The ledger and original index HTML remain under `/srv/freediving-ingest/runs/20260926-b07-cmas-index`; the HTML is omitted from the portable bundle because it embeds ephemeral CSRF fields. The isolated custom backup SHA-256 is `ecc08de5f4cd9f5ae6e4af6f9732ca9ff31daad8f01ebdb4ca2ce1faad637358`. The verified 75-file portable bundle is `/srv/freediving-ingest/bundles/20260926-b07-cmas-index`, index SHA-256 `73927a6d38768a7f9d4158209d9560365f044c7566ba681358eac499cad29958`; path, credential and session scans found no issue. Owner review and publication remain blocked. The wider 2025-onward discovery and remaining sources stay open in [issue #16](https://github.com/jamiepratt/freediving-results/issues/16).

### Three remaining official CMAS 2025 index links, September 2026 remote batch

On 26 September 2026, isolated OVH run `20260926-b08-cmas-three` reused the verified b07 official index receipt, without a new index request. Its three remaining 2025 URLs had no exact match in earlier worker acquisitions or the tracked #8 inventory; #8 private bytes were not exhaustively available for comparison. A private preflight was recorded at 13:53:08 UTC before three paced publisher GETs. It contained actual worker versions and paths but omitted an explicit cutoff field. A supplement records that omission and the actual acquisition cutoff, 13:53:31 UTC. All three URLs returned HTTP 200 `application/pdf` at their requested URL on the first attempt, without redirect or retry.

| Official PDF | Bytes, SHA-256 | Result |
| --- | --- | --- |
| [Deep Dominica](https://www.cmas.org/document/2025,-deep-dominica-depth-competition/download.html) | 609,026; `462d904bb15c029b6dd2c83eaa1942f0c41d73e85fb601fa4d3a4dd0a15b1abc` | 75/75 printed positions imported, 47/28 by physical page. The `OPENER` row retains an explicit identity uncertainty. |
| [Vertical Blue](https://www.cmas.org/document/2025-cmas-vertical-blue,-long-island,-bahamas/download.html) | 915,202; `7b8b9b51b3adb1dcd87559e0366758f4d477275b96cdf7ab484bf57c2dff88f6` | 79/79 printed positions imported, 7/7/15/6/10/9/12/4/9 by page. The day 7 header says July 9 while its footer says July 7; both remain visible. |
| [Camotes Freediving Challenge](https://www.cmas.org/document/camotes-freediving-challenge-2025/download.html) | 627,328; `4853df5a88f889f579a6e99988e27794da6c4003ce5c74ae4b806899a38017d6` | Four image-only result pages. Archived as unsupported; source row count is unknown pending OCR and independent reconciliation. |

Independent private ledgers match every imported page, line, exact source text, date and core row, with no missing or extra positions. Rendered checks covered Deep Dominica page 1, Vertical Blue pages 3, 6 and 9, and the scanned Camotes page 1. Source-bound replay preserves original values, names, statuses and uncertainty. The first Vertical Blue `/1` derivation failed the importer raw-line contract before database import; its archive history is retained, and corrected `/2` imported after independent citation checks. Repeated acquisition reused bytes, both current extractions skipped, `source-run/v1` checkpoints remained complete, direct database imports skipped, and the isolated corpus stayed at ten jobs and 978 versioned observations. These are not proven unique sporting attempts.

Private receipts, row ledgers and inventory are at `/srv/freediving-ingest/runs/20260926-b08-cmas-three`. The PostgreSQL custom backup is 428,487 bytes, SHA-256 `2d1b70d049c93e9a258ca114cb3675c191545e4f504c75e3267de9b00f28e3e6`; `pg_restore --list` passed. The 80-file portable bundle at `/srv/freediving-ingest/bundles/20260926-b08-cmas-three` verified and restored to a fresh private directory; its index SHA-256 is `996a0ee997e1bc63f0dc79c0918636f083290340001b7225b290b771f7a95dfa`. Its path and content scan found no credential or session material. The reused index HTML remains private and outside the bundle because it embeds ephemeral CSRF fields. The 15-link CMAS ledger now marks seven imported, five separately inventoried under #8 with byte equivalence unverified, and three unsupported image scans. The wider 2025-onward sweep, identity review and publication remain open under [issue #16](https://github.com/jamiepratt/freediving-results/issues/16).

### Camotes scanned PDFs, September 2026 remote batch

Isolated OVH run `20260926-b09-cmas-scans` reused the original b07 Camotes World Cup and b08 Camotes Challenge PDFs by their recorded SHA-256 values. No publisher request or source revision check occurred. The private preflight cutoff was 26 September 2026 at 14:28:46 UTC. The worker initially lacked OCR; Tesseract 5.3.4 was installed and recorded in a later supplement. Poppler rendered all eight result pages. OCR TSV files are locators only, never accepted row values.

The World Cup has 146 printed rows across four pages (38/37/37/34). Every position has a page, camera-break block, line and bounded image region in the private ledger. OCR omits cards and misreads some names and depths; pages 2-4 print no date. All 146 rows remain unresolved and none were imported. `scripts/cmas_scan_evidence.py` verifies the source hash, row positions and counts, render hashes and OCR locator hashes, then reports `import_ready: false` while unresolved rows remain.

The Challenge has 66 printed rows (17/17/17/15). Independent image review produced a private 66-row transcription ledger. Its first-page row 15 has a clipped given name, and page 3 prints no date or day. Those 18 rows remain explicit unresolved positions. Parser `cmas-camotes-challenge-2025-image/1` imported only the other 48 rows (16/17/0/15), preserving printed names, countries, AP/RP metres, times, cards, records, points, blank cells and unknown gender/category. Its archive derivation retains the exact ledger hash; import validation replays the archived PDF and ledger. All four 400 DPI page render hashes matched the independent audit. The database's 48 candidate payloads matched the derived artifact exactly. Unchanged extraction, source-run checkpoint and direct database import replays skipped. The isolated corpus moved from ten jobs and 978 versioned observations to eleven jobs and 1,026; these are not unique sporting-attempt counts.

Private evidence, preflight, inventory and backup are under `/srv/freediving-ingest/runs/20260926-b09-cmas-scans`. The Challenge ledger SHA-256 is `111cc430e79293b373fc71d64ed1d49b30f7e1ec06745771dbffa8be6af1c7b8`; its extraction job is `85b44da5049394ac01a8fa3ba4bee693b3dd81fda2956f20897d1f0e45b5e979`. The custom PostgreSQL backup SHA-256 is `cd35f834c997bd33abd8438314daa0fd79524115e746764c7c1fc6b6d9fb4d48`; `pg_restore --list` passed. The 96-file portable bundle at `/srv/freediving-ingest/bundles/20260926-b09-cmas-scans` verified and restored to a fresh private directory; index SHA-256 `6468f1efdc944f1f455e6927a2071ac887534b5afb25694ccb5be8aad0a028b0`. Its path and content scan found no credential or session material. Identity review, source validation and publication remain separate. The wider discovery sweep and unresolved scan rows remain open under [issue #16](https://github.com/jamiepratt/freediving-results/issues/16).

### CMAS 2026 official index, September 2026 remote batch

On 26 September 2026, isolated OVH run `20260926-b10-cmas-2026-index` refreshed the [official CMAS results index](https://www.cmas.org/freediving/results.html) through the shared lease. The complete HTTP 200 HTML response was 261,617 bytes, SHA-256 `34d1992f0bd75548d544d2fe4de3ffd4f711d7d530c416c766971ddf359a378d`; there was no redirect. Discovery covered titles marked 2026, from 1 January through the recorded cutoff `2026-09-26T15:03:01Z`. The 14 indexed document links have two imported, five already present in the B05 or #8 inventories, one unsupported download, and six unresolved without retrieval in this bounded selection. The private inventory retains every title, link, status and reason. The index prints a year, not event dates or full session, discipline and category inventories; those remain unknown for uninspected links. No linked national or organizer result index was selected, and this is not a global 2026 census.

Three selected document links were requested once each after private worker preflight. [Belgrade Freediving Open](https://www.cmas.org/document/2026,-belgrade-freediving-open/download.html) returned HTTP 200 `application/pdf`, 126,499 bytes, SHA-256 `336a72bb0938dfab7085b0bd3fde2f768cc5028f7897391989b1012bd2f9e146`. Its 25 April results contain 57 printed positions, reconciled 17/12/20/8 by page across DNF, SPE 2x50, DYNBF and DYN, with female and male sections. Forty-three results have numeric or time values, ten print DNS, and four print DSQ SP. One split club remains unknown with adjacent source evidence retained. [Deep Dominica 2026](https://www.cmas.org/document/2026,-deep-dominica-depth-competition/download.html) returned HTTP 200 `application/pdf`, 1,317,627 bytes, SHA-256 `1ee31123009acfa319a790debc7ecf76f9bf8c16fc53ee99486beb37af21912a`. It has 30 printed positions across July 28, 29 and 31 result tables on pages 3-8, five per page; pages 5 and 7 repeat page 4, and page 8 repeats page 6. All 30 source positions remain cited. Its PDF omits the year, category and unit; the year is index context, and no attempt deduplication is inferred. The [Nordic Cup link](https://www.cmas.org/document/2026,-nordic-cup-freediving-depth/download.html) returned HTTP 200 but failed the PDF signature check. The private gap receipt retains this status; no result rows were inferred.

Both valid PDFs were registered with original bytes, source versions and complete requested/final URL, MIME, hash and retrieval receipts. Source-bound parsers imported 57 and 30 unreviewed rows into the isolated database. Independent text ledgers and database inspection matched all 87 page, line and exact text citations, with no missing or extra position. The corpus rose from 11 jobs and 1,026 versioned observations to 13 jobs and 1,113. Unchanged acquisitions reused local bytes, extraction jobs skipped, and `source-run/v1` imports remained complete on replay. An initial Belgrade parser artifact failed evidence validation before database import; the corrected parser version `/2` created a separate immutable job, while the failed derivation remains private history.

Private originals, ledgers, preflight, inventory and the custom PostgreSQL backup are in `/srv/freediving-ingest/runs/20260926-b10-cmas-2026-index`. The backup SHA-256 is `be01151553dee1532660779d1f88f17029128ecaeb69cce319d6825e5eab25f6`. The verified 84-file portable bundle is `/srv/freediving-ingest/bundles/20260926-b10-cmas-2026-index`, index SHA-256 `27900b4b5381f558064ef867ea4b6a311b288657bb71c69e2c3aa7507859904e`; restore copied all 84 files and the secret scan found no matches. Original index HTML stays private outside the bundle because it contains ephemeral CSRF fields. Source revision relationships, sporting-attempt equivalence, owner review and publication remain unresolved under [issue #16](https://github.com/jamiepratt/freediving-results/issues/16).

### Three CMAS 2026 World Cup links, September 2026 remote batch

Isolated OVH run `20260926-b11-cmas-2026-worldcups` reused the verified b10 index, SHA-256 `34d1992f0bd75548d544d2fe4de3ffd4f711d7d530c416c766971ddf359a378d`, without checking index freshness. Its scope was exactly index positions 3-5. The worker recorded private preflight before three paced requests; the initial record's Java and Chromium versions and processing checkout were corrected in a supplement before those requests. The batch cutoff was `2026-09-26T15:42:02Z`. These links had no exact match in earlier worker acquisition receipts or the tracked #8 inventory; #8 private bytes were not exhaustively compared.

| Official link | Retrieval and disposition |
| --- | --- |
| [Indoor World Cup, Łódź](https://www.cmas.org/document/2026,-cmas-world-cup-freediving-indoor/download.html) | HTTP 200 `application/pdf`, 958,979 bytes, SHA-256 `8e8e8eb1c88e9b9ceb57a0e598a20897307790c0fc02e23c57593af9307ec623`; no redirect, one attempt. Fourteen final result pages from 13-15 March contain 118 printed positions, including 101 ranked and 17 unranked red-card rows. All 118 were imported with exact page, line and text citations. Three blank country cells remain unknown. |
| [Depth World Cup](https://www.cmas.org/document/2026,-cmas-world-cup-freediving-depth/download.html) | HTTP 200 `application/pdf`, 1,462,752 bytes, SHA-256 `1d98a22d67708f4a31ce666b84af1e81aba342c9c52e34a2e2c980cb3f010db5`; no redirect, one attempt. Four image-only pages contain 126 visually counted positions (33/32/31/30). Original bytes are archived. Every position has a private page and image-region gap citation; OCR corrupts result fields, so none were imported. |
| [Vertical Blue link](https://www.cmas.org/document/2026-cmas-world-cup-freediving-depth-vertical-blue/download.html) | HTTP 200 but invalid PDF signature. The rejecting acquisition client kept a private gap receipt with status and reason, without retaining response bytes, MIME, length or hash. No result rows were inferred. |

The Łódź parser `cmas-lodz-indoor-world-cup-2026/1` preserves printed result, card, note, date, discipline, category and uncertainty. Its independent 118-row ledger matched all 118 database candidates exactly, with no missing or extra citations. Acquisition reused the verified original, extraction skipped its existing job, `source-run/v1` remained complete, and direct import skipped on replay. The isolated corpus rose from 13 jobs and 1,113 versioned observations to 14 jobs and 1,231; these are not unique sporting-attempt counts. The updated 14-link index inventory has three imported, five previously inventoried, three unsupported and three unrequested unresolved links.

Private evidence and backup are in `/srv/freediving-ingest/runs/20260926-b11-cmas-2026-worldcups`; the inventory SHA-256 is `9dd0f92a5451c8098b5d644df0c8759284b3da43bcb8ada6cfded22a1315225d`, and the custom backup SHA-256 is `1d16c8b92f77d1dc38f5b43ec683461cea890ef91214d5f7cf7064fc022ea497`. The 99-file bundle at `/srv/freediving-ingest/bundles/20260926-b11-cmas-2026-worldcups` verified and restored, index SHA-256 `ed58509d5bcfd61219ffe25d39d6d984786f313c2d93ba870b2b831960480c42`; path and content secret scans found no matches. The depth scan, invalid linked download, remaining index links, wider 2025-onward discovery, owner review and publication remain open under [issue #16](https://github.com/jamiepratt/freediving-results/issues/16).

## Reuse a verified private PDF bundle

For an already acquired PDF, run `python3 scripts/reuse_portable_source.py BUNDLE SOURCE_SHA256 ACQUISITION_ID PRIVATE_EXPORT_DIR` from the repository checkout. It verifies the complete portable bundle and the canonical archive acquisition, then exports only the original PDF, its archive manifest, and a sanitized provenance JSON file. The destination must be new and private. The JSON marks transfer as reuse and leaves HTTP status unverified when the original acquisition lacks an HTTP receipt. Transfer these three files to the isolated worker, verify the PDF hash there, and register the original manifest with `clojure -M:archive import`. Do not describe the transfer as a fresh publisher observation.

On 26 September 2026, isolated OVH run `20260926-b05-reuse` used this route for the 2026 Croatian Open National and Italian Open Outdoor PDFs from the verified B29 bundles. Original source hashes are `e57141f79dfe38d12aaab20d04ad44d506a60ba0e1209968beb6f5037d0bf615` and `b29ee59117eedefd4784840be9386b9b7e5cfa86bbb89c18825220b6e2fb5d88`; original acquisition IDs are `f64c29892843a98f5c26c73394998f76cd8c37a77a5bc806d7d83f052fba9311` and `51dfc7c2133b12b5f077cb16429d7e70a8e3e47368af7769a86c15f42c356471`. The isolated corpus imported 246 and 183 cited result positions, each matching the independent B29 ledger by page, line and exact text. Both extraction and database import replay skipped. The corpus then held six jobs and 684 versioned observation rows, not established unique sporting attempts. The verified 39-file private bundle is `/srv/freediving-ingest/bundles/20260926-b05-reuse`, index SHA-256 `e4fb496ac77b3f72f36345e7af9095c6fd523b615c5d9e3e46b989e612aa8d76`. Original Chrome acquisition records lack independent HTTP status and byte-length receipts; no publisher request or revision check occurred in this run. Review, held-out eligibility, and publication remain separate under [issue #16](https://github.com/jamiepratt/freediving-results/issues/16).

## Paced acquisition entry points

Use `scripts/acquire_source.py` for official discovery pages, PDFs, JSON and HTML. Supply the expected representation explicitly. Its private output retains exact successful bytes, hash, MIME, status, timing, redirect chain and host-only pacing events. A restriction, challenge page, wrong MIME or invalid signature produces a host-only coverage gap and no source object. This command does not register or ingest a source.

```sh
python3 scripts/acquire_source.py https://www.cmas.org/ html data/private-acquisition --dry-run
python3 scripts/acquire_source.py OFFICIAL_URL pdf data/private-acquisition
python3 scripts/acquire_source.py OFFICIAL_URL pdf data/private-acquisition --archive-root /private/mounted-archive --context-json '{"event":"exact-event","version":"exact-result-version"}'
python3 scripts/acquire_source.py OFFICIAL_URL pdf data/private-acquisition --archive-root /private/mounted-archive --context-json '{"event":"exact-event","version":"exact-result-version"}' --refresh
```

The HTTP command inventories its output directory and every configured existing acquisition root before a request. A root may be local or a mounted remote private directory. It checks the exact requested URL, representation and supplied JSON date/filter/version context; HTTP 200, MIME, final URL and redirect chain; timestamps with offsets, byte length, SHA-256 and response signature; and the complete `source-run/v1` checkpoint for new receipts. Corrupt, missing or incomplete candidates are ignored. An unavailable or corrupt configured root stops the command. Matching verified bytes are copied atomically into the output directory with unchanged original acquisition provenance and a new run reference. The default receipt says `freshness: not_checked`. `--refresh` makes a new paced publisher request and records `freshness: refresh_requested`; neither label proves the publisher has no later revision. Compare the observed SHA-256 and selected context before asserting any revision relationship. Existing flat legacy acquisition receipts can be reused only when they contain the same complete retrieval evidence.

For a configured Clojure archive with `objects/` and `acquisitions/`, the command runs the versioned `clojure -M:archive inventory-json ARCHIVE` API first. This verifies every manifest identity, object hash and retained browser evidence. Matching records count as `legacy_archive_incomplete` in the receipt because those manifests alone lack observed HTTP status and byte length. They are not reused until paired with a complete acquisition receipt. A corrupt archive fails closed before any publisher request. A mounted remote root must already be accessible; this command does not discover or authenticate to remote services.

Each successful request or reuse writes `run-<identity>.json` with a complete source checkpoint and pending import. The source and provenance files are staged with atomic replacement; a failed or partial HTTP response creates a gap with the source identity hash, host, status and safe retry events, but no complete checkpoint. A downstream importer can use the public `import_once(run_path, importer)` API, which verifies source bytes and provenance again, invokes the importer's existing idempotent archive/extraction/observation APIs, and atomically records its immutable job reference only after success. A crash before the checkpoint retries that idempotent importer on restart. The CLI form executes an exact command once after substitution of `{source}` and `{provenance}` and requires the caller's immutable job reference:

```sh
python3 scripts/acquire_source.py import-once data/private-acquisition/run-IDENTITY.json JOB_ID -- clojure -M:observations import data/private-archive JOB_ID
```

This example assumes the exact verified source and provenance were already registered and extracted in that archive, and the job ID is the deterministic extraction receipt. Supply `FREEDIVING_DATABASE_URL` for the isolated database. Verify the job ID and source hash against the extraction receipt before calling it. This checkpoint prevents repeated successful invocation after restart; it cannot by itself make an external, non-idempotent importer safe after a crash between external commit and checkpoint. The existing archive registration, derivation and observation import APIs provide that idempotence.

Use `scripts/capture_browser.py` for a page that needs browser rendering. It requires Playwright and a Chromium installation; `--channel chrome` uses an installed Chrome channel. The command routes navigation, API calls, direct downloads and page assets through the same per-host lease. Routed GET and POST responses use a bounded HTTP read before browser fulfillment, including when the publisher omits Content-Length. Unsupported methods or compressed responses produce a safe gap. POST is attempted once because replay after an uncertain response could duplicate a publisher action. It retains result-bearing response bytes, safe request URLs and redirect targets, and the complete rendered DOM under a new private output directory, or a safe coverage gap on a terminal failure. Blocked service workers prevent requests from bypassing the route. Browser capture does not infer selected dates, filters, source identity or publisher authority; record and verify those before archive registration.

```sh
python3 scripts/capture_browser.py https://www.aidainternational.org/StartList/4349 data/private-browser --dry-run
python3 scripts/capture_browser.py OFFICIAL_PAGE_URL data/private-browser
python3 scripts/capture_browser.py OFFICIAL_PAGE_URL data/private-browser --archive-root /private/mounted-archive --context-json '{"selected_date":"2025-06-28"}'
```

For Mabini Depth Quest May 2, use `--selection-file config/aida-mabini-2025-05-02.selection.json` with `https://www.aidainternational.org/Events/EventResults-4545`. The config clicks `#day_2` and checks the active date and result rows. Its `2025-05-02` response marker only proves that a retained response contains that date; before registering or importing, compare the complete retained May 2 result-bearing response table against the captured DOM row by row, including the 12-column attempt cells, and record any mismatch as a gap. A dry run validates the config without contacting AIDA.

With `--archive-root`, browser capture verifies configured Clojure archives before navigation and checks exact URL, representation and context against complete flat acquisition receipts for each routed GET. Requests carrying cookies or authorization headers bypass reuse because the receipt cannot prove a matching session. Verified responses are fulfilled without another publisher request; `capture.json` records their original acquisition path and retrieval time as a new run reference with `freshness: not_checked`, plus host-only pacing and retry events. Gaps retain those safe events. Use `--refresh` to bypass response reuse. Historical Clojure manifests without complete HTTP facts remain inventory evidence only. Browser request headers, including session cookies, are forwarded in memory to the bounded reader; no headers are written to the capture index.

Both commands use `scripts/source_acquisition.py`. Its SQLite lease under `~/.local/state/freediving-results/acquisition.sqlite3` coordinates workers and restarted processes on the same machine. Configure a single shared `lease_path` in Python callers, or `--lease-path` for both HTTP and browser capture CLIs, when using another private state location. Every HTTP retry and redirect host gets a fresh lease. The default policy is two concurrent requests per host, at least one second between starts, a 20-second timeout, three attempts, 30-second maximum retry delay, five redirects and 50 MiB response limit. CMAS hosts default to one concurrent request, three seconds between starts and two attempts. A caller can set stricter host policies. `Retry-After` is honored up to the configured maximum. Events and terminal errors omit URL paths, queries, response bodies and headers. Do not put credentials or session-bearing URLs in command arguments or private provenance.

The historical manual and ad hoc acquisition paths are not automatically guarded by these commands. Future #8/#16 acquisition must use these entry points or the shared client and verify the resulting evidence before archive registration. Check the [pacing prerequisite #22](https://github.com/jamiepratt/freediving-results/issues/22) and current source inventory before starting another ingestion batch.

## Find and acquire a source

| Source and discovery | Result route and representation | Observed access and context | Provenance caution |
| --- | --- | --- | --- |
| CMAS 2026 Novi Sad [seniors/juniors](https://www.cmas.org/document/2026,-cmas-world-championship-freediving-indoor/download.html) and [masters](https://www.cmas.org/document/2026,-cmas-world-championship-freediving-indoor-masters/download.html) | Direct `download.html` PDFs | Senior PDF returned 200 `application/pdf`, including anonymous Python HTTPS GET; browser download also worked. Both PDFs were archived with valid `%PDF-` signatures. | A browser success does not establish a login requirement. Check status, MIME, signature, length and hash for each retrieval. |
| CMAS 2025 Mytikas federation [CWT men](https://www.cmas.org/media/com_eventbooking/Result_CWT-MS_M.pdf) and [women](https://www.cmas.org/media/com_eventbooking/Result_CWT-WS_W.pdf) | Direct federation PDFs | Acquired as PDFs; the event archive does not directly expose the men's file. | The federation copies and timing-service CWT copies differ in bytes. No row-level revision relationship follows from the filenames or hashes. |
| CMAS 2025 [depth](https://www.cmas.org/document/2025,-cmas-world-championship-freediving-depth/download.html) and [masters](https://www.cmas.org/document/2025,-cmas-world-championship-freediving-depth-masters/download.html) | Federation redirect to Microplus `#/competition-schedule/3`; [competition 3 API](https://cmas-api.microplustimingservices.com/api/competitions/3), schedule, unit results and documents lead to `RES` PDFs | Twenty result PDFs acquired. The masters redirect to `/event-detail/4` is stale; competition 4 is another event. | Keep federation link and redirect chain. Distinguish `RES` result documents from start lists and medalist files. |
| CMAS 2026 [Roatan depth](https://www.cmas.org/freediving-events/2026-cmas-world-championship-freediving-depth.html) | [competition 30 API](https://cmas-api.microplustimingservices.com/api/competitions/30), unit result/document JSON and timing PDFs | Seven result PDFs acquired. CWT men unit 3551 has no linked PDF; the linked unit 3559 result returned 404. Later direct API and app probes returned 403. | Retain failed response metadata as a gap. Unit JSON rows and a medalists PDF do not fill missing attempt-result PDF coverage. |
| CMAS 2025 Athens [event](https://www.cmas.org/freediving-events/2025-cmas-world-championship-freediving-indoor.html) and indoor archive entries | Archive redirects to legacy selectors `#/2/schedule-bydate` and `#/1/schedule-bydate`. A human [DNF result page](https://results.microplustimingservices.com/CMAS/Results/#/2/dynamic-result-json/MAM/011/007/001) cites a distinct [CGR1 JSON response](https://results.microplustimingservices.com/CMAS/ExportPOST/export/CMAS_2/TFMAM011CLAS07%20001.JSON). | Forty-two Results JSON views acquired across seven disciplines and six category codes. All 30 configured result PDF links returned 404. Later direct federation-page visits returned 403. | Both selectors contain junior, senior and master categories; they separate discipline groups. Keep selector, page fragment, JSON URL and exact JSON bytes. HTML is a citation, not the archived JSON object. |
| AIDA official [Wakayama 4349](https://www.aidainternational.org/StartList/4349), [Limassol 4350](https://www.aidainternational.org/StartList/4350), [Budapest 4852](https://www.aidainternational.org/StartList/4852) | Selected-date HTML attempt tables at a URL that may remain unchanged; a literal `day_index` query can also select a date. | Browser access worked; scripted direct HTTP returned 403 in discovery. Sixteen distinct date views were retained. | Capture selected date, filters, response HTML and complete rendered DOM. Ranking tables are supplemental views, not additional attempts. Browser success does not prove login was required or absent. |
| Secondary publications: [Sportalsub Wakayama DYN export](https://www.sportalsub.net/en/wp-content/uploads/2025/07/DYN-RESULTS.pdf), [Venezuelan federation Athens PDF](https://www.fvas.com.ve/wp-content/uploads/2025/05/Resultados-CMAS-Freediving-Indoor-Atenas-2025.pdf), [Sportalsub World Games resultbook](https://www.sportalsub.net/blog/wp-content/uploads/2025/08/Full-Results-Freediving-WG-2025.pdf) | PDF mirrors | Acquired as supporting copies. The [official World Games PDF](https://www.cmas.org/document/2025,-the-world-games/download.html) was acquired; the [Tissot resultbook landing](https://swog2025.theworldgames.org/nh/en/Pdf/Resultbook) was visible, but its official zip/download returned 403 or no permission in observed clients. | Record publisher and `:mirror-of`; do not claim independent corroboration, equivalent bytes or a verified before/after revision. The Athens PDF materially disagrees with current JSON. |

For Microplus, inspect the schedule, unit results and document API responses before selecting a PDF. Record the competition, discipline, category, round and heat. In Athens, `CGR1` is Results; do not double-count Summary, responsive mobile rows, or start-list exports. The 42 archived views contain 792 original JSON positions. One DNF senior-women response has an invalid UTF-8 byte at position 19: retain its original bytes and quarantine that position, without guessing a name or recoding the file. The B30 parser imported 791 positions, plus one earlier parser version for the same 30-row source. These counts do not establish event-wide completeness or distinct sporting attempts.

## Register evidence, not a filename

Use the [archive manifest contract](../README.md#manifest-contract) and its synthetic fixture. Before registration, retain the HTTP request/discovery URL, each redirect and final URL, retrieval time, status, MIME, byte length and SHA-256. Verify a PDF starts with `%PDF-`; a `download.html` suffix or `.pdf` filename is insufficient. Save original successful PDF, JSON or HTML response bytes in the ignored private archive. Keep failed 403/404 bodies and headers in private acquisition evidence rather than registering them as result sources.

The manifest requires `:sha256`, `:discovery-url`, `:final-url`, `:acquisition-method`, `:retrieved-at`, `:content-type`, `:publisher`, `:relationship` and `:mirror-of`. Use optional `:provenance` for `:publisher-url`, full `:redirect-chain` and browser state. For AIDA, retain the selected date/filter and a separately hashed, complete rendered DOM with `freediving.archive/retain-evidence!`; compare it with result-bearing response cells. The earlier truncated DOM captures were hash-valid but incomplete, so later complete captures remain separate acquisition versions. For Microplus, keep meaningful URL fragments and supported query context; do not strip them to satisfy URL validation. Never save credentials, cookies or session-bearing URLs.

`clojure -M:archive import ARCHIVE SOURCE MANIFEST.edn` returns a content SHA-256 and acquisition ID. `clojure -M:archive inspect ARCHIVE SOURCE_SHA256` verifies the object and returns its private `:artifact-path` and every acquisition manifest for that content. A source hash identifies bytes; an acquisition ID identifies a particular retrieval and context. Repeated identical imports skip; distinct timestamps, redirects or selected views may create distinct acquisitions for the same bytes. Neither changed hashes, `-v2` filenames, parser versions nor retrieval order alone prove a publisher revision or a distinct sporting attempt. Preserve possible and confirmed relationships separately under [revision evidence rules](revision-relationships.md).

## Trace one observation back to its source

Select the **isolated** database and its matching private archive first. The database contains immutable extraction artifact bytes and candidate payloads, but a dump alone does not contain the original source object archive. With an exact `JOB_ID` and zero-based observation `ORDINAL`, set `psql` variables `job_id` and `ordinal` using `-v`, then use a read-only SQL session:

```sql
BEGIN READ ONLY;
SELECT o.job_id, o.ordinal, o.candidate_id, o.kind, o.payload_edn,
       e.source_sha256, e.artifact_sha256, e.parser_version,
       e.schema_version, e.artifact_bytes
FROM freediving.observations AS o
JOIN freediving.extractions AS e USING (job_id)
WHERE o.job_id = :'job_id' AND o.ordinal = :ordinal;
ROLLBACK;
```

The public project API `(freediving.observations/inspect jdbc-url job-id)` returns the stored artifact and ordered observations; `clojure -M:observations inspect JOB_ID` is the CLI form. Match the exact ordinal and candidate ID, then verify the stored artifact bytes against `artifact_sha256` and its `source-sha256` against `source_sha256`. The archive's `derivations/JOB_ID.edn` receipt points to the immutable derived artifact. In the matching archive, run `clojure -M:archive inspect ARCHIVE SOURCE_SHA256`; inspect each returned acquisition manifest for discovery/final URLs, redirects, publisher, selected context and acquisition ID. Match the artifact's retained acquisition snapshot to those IDs, since the archive can hold later acquisitions of the same bytes. Verify the returned `:artifact-path` bytes against `SOURCE_SHA256`. Database `inspect` does not itself replay the source parser; the importer's unchanged retry does. See [local PostgreSQL APIs](../README.md#local-postgresql) and [private source inspection](local-source-inspection.md) for authenticated row viewing and replay checks.

Read `:coordinates` from the exact candidate in `payload_edn` or the inspected artifact. Schema 5 JSON uses `:row-index-zero-based` in the original `CGR1` array, not a visible page number. PDF schemas 1-3 use 1-based `:page` and `:line` in extracted text; retain rendered page evidence when line order is ambiguous. Schema 4 AIDA HTML uses 1-based `:table` and `:row`, paired with selected date/filter and retained table cells. `ordinal` is the database candidate order and is not interchangeable with a JSON array index. Cite the human HTML page for JSON plus its exact response URL and position; cite PDF page/line or AIDA date/table/row for those sources.

| Isolated corpus at the 25 September snapshot | Registered/imported source membership | Row/version meaning and private navigation |
| --- | --- | --- |
| B11 `championship_b11`; B16 `championship_b16` restore of B11 | B11: 24 AIDA acquisition versions, 20 CMAS 2025 depth timing PDFs, two 2025 federation CWT PDFs, seven 2026 Roatan depth PDFs and two 2026 Novi Sad indoor PDFs. B16 adds two parser-2 jobs over those same indoor PDFs. | B11 has 55 extraction versions and 4,736 observations; B16 has 57 and 5,384. These are versioned rows, not unique attempts. See [corpus audit](championship-corpus-20260925.md) and private `data/b11-evidence/OPERATIONS.md`, `import-manifest.json`, `review-index.jsonl`, and B16 `data/b16-evidence/OPERATIONS.md`, `review-index.jsonl`. |
| B30 `championship_b30_4x50`, separate Athens corpus | Forty-two distinct official `CGR1` response hashes, 792 source positions: 791 candidates and one byte-quarantined position. | Forty-three extraction versions and 821 versioned observations include a second parser version of one 30-row source. See [JSON ingestion audit](cmas-2025-json-ingestion.md) and ignored `data/b30-2025-4x50/` archive, ledgers and backup. Athens JSON is absent from B11/B16 and production. |
| Croatian Open 2026, separate private held-out candidate corpus | One official CMAS-hosted PDF, SHA-256 `e57141f79dfe38d12aaab20d04ad44d506a60ba0e1209968beb6f5037d0bf615`. The Chrome `View` route supplied the PDF; the retained macOS download provenance names that route and its CMAS detail page. | Parser `cmas-croatia-open-2026/1` preserves 246 result rows across eight pages with exact source fields and page/line evidence. All 246 imported as one unreviewed extraction version; unchanged extraction and import retries skipped. The ignored `data/croatia-2026-source/` archive and database and `data/croatia-2026-audit/` ledger are local private evidence. This corpus has no owner labels, selected identity cases or publication approval. |

Private paths above identify packet locations in their retained batch checkouts; they are not Git files or portable paths. B32 Roatan CWT-men evidence is a separate **source-gap audit** of unit JSON and 403s, not an imported replacement corpus; see [source gaps](championship-source-gaps-20260925.md#roatan-cwt-men-coverage). No listed isolated corpus is production; acquisition and import do not grant review, identity, selection or publication authority.

## Portable private corpus bundles

`python3 scripts/portable_corpus.py export SPEC.json BUNDLE` copies an explicit file map into `BUNDLE/payload/` and writes `BUNDLE/index.json`. Keep the spec and bundle in a private destination outside managed worktrees. The index records portable paths, role, byte count and SHA-256 for every file, plus corpus metadata. It does not retain original absolute source paths. A directory entry includes every regular file recursively, including PDFs, JSON, HTML, rendered DOM evidence, acquisition manifests, derivations, row ledgers, review records and a custom PostgreSQL backup when those are supplied. Empty directories are retained. Symlinks, path traversal, duplicate paths, missing files and changed bytes are rejected. The exporter never removes or changes the source files.

Example private spec shape (replace paths, hashes and metadata with the exact corpus being exported):

```json
{
  "schema": "portable-corpus-spec/v1",
  "corpus": "isolated-example",
  "entries": [
    {"source": "/absolute/private/archive", "path": "archive", "role": "acquisition-and-extraction-archive"},
    {"source": "/absolute/private/review", "path": "review", "role": "append-only-review-evidence"},
    {"source": "/absolute/private/database.dump", "path": "database/database.dump", "role": "postgres-custom-backup"}
  ],
  "archive_roots": ["archive"],
  "receipt_paths": ["review/owner-decision-receipt.json"],
  "references": [{"path": "review/source.json", "sha256": "replace-with-64-hex-source-hash"}],
  "metadata": {
    "scope": "exact isolated corpus, event, session and acquisition coverage",
    "database_format": "pg_dump custom",
    "restore_command": "createdb NEW_ISOLATED_DB && pg_restore --no-owner --no-acl --dbname=NEW_ISOLATED_DB database/database.dump",
    "import_notes": "Record parser version, source hashes and job IDs; replay only into an isolated database"
  }
}
```

`archive_roots` checks that every acquisition manifest names a bundled source object under `objects/<sha256>` and every derivation receipt names a bundled artifact under `derived-objects/<artifact-sha256>`. `receipt_paths` checks a JSON owner receipt's `source` and `result_files` references against bundled bytes. `references` asserts other exact path/hash relationships, including source and artifact hashes cited by row evidence. These checks run again on `verify` and before `restore`. Keep a separate trusted copy of the printed `index_sha256` so a changed index is detectable; SHA-256 is an integrity check, not an author signature.

Run `python3 scripts/portable_corpus.py verify BUNDLE` after copying the bundle. Run `python3 scripts/portable_corpus.py restore BUNDLE FRESH_TARGET` to copy verified payload bytes to a new, nonexistent directory. The restore checks the copied bytes before reporting success and sets private directory/file modes to `0700`/`0600`. The restored `archive/` is then usable by the existing archive and observation CLIs. Restore the database dump with the recorded command into a **new isolated** PostgreSQL database, then compare database extraction jobs, observation counts and review decisions with the retained ledgers. Neither a successful bundle restore nor a database restore approves identity, result selection or publication. Do not include a live PostgreSQL data directory, credentials, cookies, session URLs or raw sources in Git.

## Detailed evidence

[Dated championship inventory](championship-inventory-20260925.md), [CMAS timing acquisition](cmas-timing-acquisition-20260925.md), [Athens JSON ingestion](cmas-2025-json-ingestion.md), [AIDA HTML ingestion](aida-html-ingestion.md), [source gaps](championship-source-gaps-20260925.md), and [Athens mirror audit](athens-mirror-audit-20260925.md) contain the full reconciliations and known limitations. Keep private names, raw bytes, manifests and row ledgers under ignored `data/`; record future acquisition and coverage work in [issue #8](https://github.com/jamiepratt/freediving-results/issues/8) or [issue #10](https://github.com/jamiepratt/freediving-results/issues/10), not a repo-local plan file.
